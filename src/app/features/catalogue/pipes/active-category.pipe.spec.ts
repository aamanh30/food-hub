import { ActiveCategoryPipe } from './active-category.pipe';

describe('ActiveCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
